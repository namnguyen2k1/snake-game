<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <style>
      :root {
        --primary-color: #0d0b52;
        --secondary-color: #3458b9;
        --font-color: black;
        --bg-color: #ffffff;
        --primary-text: yellow;
      }
      body {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
        font-size: 20px;
        color: var(--font-color);
        background-color: var(--bg-color);
      }
      a {
        color: var(--primary-text);
      }
      .panel {
        width: 35rem;
        height: max-content;
        padding: 2rem;
        margin: 0rem 0.2rem;
        border-radius: 0.5rem;
        background-color: var(--secondary-color);
      }
      .panel-heading {
        font-weight: bold;
        font-size: 2.5rem;
        text-align: center;
      }
      .form-group {
        margin-bottom: 1rem;
      }
      label {
        font-style: italic;
        margin-bottom: 0.5rem;
        font-size: 1rem;
        padding-left: 0.5rem;
      }
      input {
        min-width: calc(100% - 2rem);
        padding: 0.5rem 1rem;
        border-radius: 0.5rem;
        border: none;
        outline: none;
        font-size: 1.5rem;
      }
      button {
        min-width: 100%;
        border-radius: 0.5rem;
        background-color: var(--primary-color);
        color: var(--primary-text);
        padding: 0.5rem 0rem;
        border: none;
      }
      .more-option {
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        align-items: center;
      }
    </style>
  </head>
  <body>
    <div class="panel panel-primary">
      <div class="panel-heading">Login</div>
      <form class="panel-body" method="post" action="/users/login">
        <div class="form-group">
          <label class="col-md-4">Email:</label><br />
          <input type="text" class="form-control" name="email" placeholder="example@abc.com" />
        </div>
        <div class="form-group">
          <label class="col-md-4">Password:</label><br />
          <input type="password" class="form-control" name="password" />
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </form>
      <div class="more-option">
        <div class="option-item">
          Don't have an account?
          <a href="/users/register">register</a>
        </div>
      </div>
    </div>
  </body>
</html>
